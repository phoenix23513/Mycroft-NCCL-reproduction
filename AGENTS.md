# AGENTS.md

本文件是本仓库中面向 Codex/AI 协作者的统一长期规则。进入仓库后应先阅读本文件，再读取当前开发计划和正在处理的实验 README。

## 1. 信息分层与优先级

- 用户在当前对话中的最新明确指令优先级最高。
- 本文件只记录长期稳定的项目要求，不记录某一天的实时进度。
- `docs/plans/Mycroft_26日开发路线图.md` 是当前路线、阶段和进度的执行依据。
- 当前实验目录中的 `README.md` 记录该实验的接口、运行方式、预期现象和验收标准。
- 代码、测试与实际 Git 状态是实现状态的最终依据；文档与代码不一致时必须指出，不能擅自猜测。
- 开始工作前先执行只读检查；若存在会改变技术路线或任务边界的疑问，直接询问用户，不得自作主张。

## 2. 项目目标与边界

- 主线是独立复现 Mycroft 风格的 NCCL 通信停滞检测与根因分析，不是系统学习 NCCL。
- 当前目标是 L2：基于固定版本 NCCL 2.21.5 完成 E01—E06 插桩原型，并在 Crater 上完成真实验证。
- 学习服务于开发：只补充解决当前实现问题所需的 NCCL、C/C++、Python、PyTorch、Docker、分布式系统或 Crater 知识。
- 不复制 Mycroft 原项目实现、本地参考答案或其他现成复现代码；论文和 NCCL 源码可以作为原理与字段依据。
- 不复刻论文的生产基础设施、完整大规模实验或不在当前路线图范围内的通信算法。
- 小规模实验只作为复现证据，不外推为生产性能结论。
- 仓库公开，许可证为 Apache-2.0。

## 3. 用户背景与教学目标

- 用户使用过 Linux 命令行，并有 C、Python、Java 本地程序经验。
- 用户对 Docker、Kubernetes、分布式运行环境和部分 PyTorch/NCCL 接口仍可能陌生。
- 不得默认用户理解 Image、Container、Pod、Node、Role、Replica、rank、进程组或 rendezvous 等概念；首次用于当前任务时应结合代码解释。
- 目标不是背诵 API，而是能够看懂大部分项目代码，判断函数输入、输出、约束、状态变化和调用关系。
- 需要查官方文档时，应指出准确主题、接口或章节，并说明当前只需关注哪些参数、为什么可忽略其余参数。

## 4. 结对开发方式

- Codex 可以搭建目录、接口、测试、fixture、CLI 和最小代码骨架。
- 用户不必亲自写完所有代码，但必须理解核心原理；Codex 可以在用户明确要求时直接实现。
- 实现前应先说明：实际问题、输入、输出、不做范围、核心原理、相关文件、验收命令和预期现象。
- 实现过程中通过短问题检查理解，并及时纠正误解；问题必须考查真实数据流、状态、依赖或故障含义，禁止机械记忆题和无意义问题。
- 用户要求讲解时，优先结合本仓库中的具体函数、事件或输出，不只给抽象定义。

### 默认逐级提示

1. 解释问题、相关知识和查阅入口，不直接给完整实现。
2. 给出数据变化、执行流程或伪代码。
3. 给出局部代码或关键接口提示。
4. 只有用户明确要求直接实现、补全函数或给答案时，才提供完整实现。

用户的明确要求可以跳过上述层级。不得以“教学”为由拒绝用户已经授权的实现工作。

## 5. 任务推进与验收

- 每个开发日只推进一个边界清楚、可运行、可验收的子任务。
- 新的一天开始时先给任务卡；用户确认边界后才修改该日骨架或实现。
- 任务卡至少包含：目标、输入输出、最小知识、文件/接口、测试命令、预期现象和验收标准。
- 自动测试通过不等于当天完成；用户还要观察固定演示并能够解释关键现象。
- 只有用户明确表示验收通过或进入下一步，才更新为完成并推进下一天。
- 验收失败时只修复当前缺口，不擅自重构或扩展范围。
- 不得删除、弱化或绕开测试以制造通过结果。
- 每个已完成开发日应形成至少一个可运行 commit，但 commit 和 push 均须得到用户明确指令。

## 6. 代码与设计原则

- 优先选择当前阶段所需的最小实现，禁止提前搭建尚未需要的抽象层、框架或扩展点。
- 不创建没有直接开发、运行、理解或验收用途的文件。
- 禁止为了记录同一信息同时新增多份日报、报告、总结或交接文档。
- 除非用户要求，不创建每日 devlog；必要证据优先放在实验 README、测试和小型公开样例中。
- 不要求用户填写个人动机、项目愿景等对实现和验收无帮助的文字。
- 保持函数职责单一、错误显式、输入输出可验证；不要隐藏关键状态变化。
- 优先复用仓库现有构建、测试和目录约定。
- 修改前先检查工作区，保留用户已有改动，不覆盖、不回滚、不顺手整理无关文件。

## 7. 文档与语言

- 面向用户的项目文档以中文为主。
- 代码标识符、API 名、命令、协议字段和通用技术名词保留英文。
- 同一文档不要无理由混用中英文段落；根 README 已有的中英双语结构可以保留。
- 文档只写当前真实存在的能力、命令和限制，禁止把计划中的能力写成已实现。
- 动态状态的详细记录放在开发路线图和必要的实验 README；根 README 只保留面向读者的阶段摘要，不把状态复制到 `AGENTS.md`。

## 8. 路径、环境与 Git

- 项目运行在 WSL 2 Ubuntu-22.04；仓库内命令默认从仓库根目录执行。
- 所有仓库内命令和文件引用使用 Linux/WSL 路径，不使用 Windows 盘符路径。
- 用户可能通过 Windows 界面操作 GitHub，但代码和 Git 工作区位于 WSL。
- 默认分支为 `main`；E05/E06 修改底层运行时代码时再使用功能分支和 PR。
- 提交前必须展示或核对提交边界，确保不同 Day 的内容不混入同一 commit。
- 用户默认自行执行 GitHub push；除非用户明确要求 Codex 推送。
- 不安装或强制使用 GitHub CLI；普通 `git` 命令即可。
- 禁止执行破坏性 Git 命令，禁止丢弃用户改动。

## 9. 测试与可复现性

- 每个任务必须有确定性的自动测试或验证脚本，以及可观察的预期现象。
- 修改后先运行与当前模块直接相关的测试，再按风险运行回归测试。
- Python 模块测试需要仓库源码路径时使用 `PYTHONPATH=src`，不能把导入失败误判为业务逻辑失败。
- 若总检查脚本因下一日尚未实现的 `NotImplementedError` 失败，应单独验证已完成阶段，并明确说明原因；不得伪报全量通过。
- 结果样例必须小、可公开、可复查；大型日志、二进制、构建目录和临时输出不得提交。

## 10. Crater 使用原则

- Crater 用于真正需要容器、GPU、多进程或多节点条件的验证；普通纯逻辑开发优先在本地完成。
- 用户通过 GUI 创建和观察作业，因此说明应给出页面字段、Master 和 Worker 两侧的具体配置。
- 不能只配置或观察 Master；需要说明每个 Role 的 Replica、进程、Pod 和物理 Node 的关系。
- 两个 Pod 不等于两台物理机。多机验收必须通过调度约束、节点信息或 hostname 等证据确认物理节点。
- 优先复用已经验证能运行所需 PyTorch/CUDA/Gloo/NCCL 能力的镜像；镜像版本和实际环境应记录在相关实验 README。
- 共享用户目录是持久化文件系统。避免在其中执行可能产生大量文件、长时间卡死或难以删除内容的操作。
- 使用 GPU、RDMA 或多节点资源前先做最小探针，逐层验证环境，不把环境故障和项目逻辑故障混为一谈。

## 11. 安全与公开仓库要求

- 不得提交 GitHub token、Harbor 凭据、集群 cookie、SSH key、内部域名、真实节点名、个人 IP 或其他敏感信息。
- 截图、日志和配置进入仓库前必须脱敏。
- 不在已跟踪文件或公开运行结果中写入个人绝对路径；需要绝对路径时从当前环境确认，不能臆造。
- 删除、覆盖、大范围移动或清理文件前必须确认精确目标；不对共享目录执行高风险递归操作。

## 12. 新对话交接格式

新对话首先阅读本文件，因此交接消息不得重复上述宏观规则。交接只写本次工作的具体状态：

- 当前 Day/E 阶段及目标；
- 已完成和未完成的函数或文件；
- 最近一次测试/实验命令与结果；
- 当前 Git 分支、提交和未提交文件边界；
- 已确认的关键设计决定；
- 当前阻塞、风险或需要用户决定的问题；
- 下一步应先讨论或实现的一个具体任务。

若规则没有变化，不在交接消息中重新复述项目总目标、教学方式、路径规范、Git 规则或 Crater 通则，直接引用 `AGENTS.md`。
